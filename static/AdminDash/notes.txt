Admin can create a budget.
Admin can See employees list



Admin can click on Payroll page 
    Should see-
        -All employees table with option to select them
            -Selected employees punch table / edit(later)
            -Payroll simulator: Admin sends payroll per employee 'date from' 'amount' 'hours' ext
            -Documentation: Documentation table gets updated dynamically from payroll
        

Employee --
    Should be able to login to dashboard with account generated by Admin
    Employee should be able to see profile page with his information dynamically generated

    Under PayRoll Should see -
        - Punch Card : Clock In, Clock Out buttons. Current Time and Date,
        - Timesheet: employees sees his own table with punches
        

Change request
    -- Employee should be able to submit a change request
        -- Admin should be able to accept or deny request
            --As admin accepts (Update database entry)
            --As admin deny (Delete request)

Pupulate a delete button with every new row in tables that once clicked lets you delete